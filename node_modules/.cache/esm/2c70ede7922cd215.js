let express,route,bodyParser;_922‍.w("express",[["default",["express"],function(v){express=v}]]);_922‍.w("./routes",[["default",["route"],function(v){route=v}]]);_922‍.w("body-parser",[["default",["bodyParser"],function(v){bodyParser=v}]]);


const app = express()
const PORT = 9000
app.use(bodyParser.json())
app.use('/api/v1/', route)
app.listen(PORT, (err, result) => {
    if (err) console.log(`Getting error`)
    else {
        console.log(`server starting at port ${PORT}`)
    }
})


var arr = [1, 2, 3, 4, 5, 6]
var ele = 4
for (var i = 0; i < arr.length; i++) {
    if (arr[i] == ele) {
        for(j=i;j<arr.length;j++){
            arr[j] = arr[j + 1]        
        }
    }
}

_922‍.g.console.log(arr)